"""Module containing all SQLalchemy Models."""

# Make sure, that all models are imported somewhere.
# If models are not imported, they will not be discovered during database
# or migration creation resulting in missing database tabes!

from . import example  # noqa
